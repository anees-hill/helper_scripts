# helper_scripts

Personal bootstrap scripts for setting up a fresh Ubuntu server.

## Fresh server usage

```bash
sudo apt update
sudo apt install -y git

git clone https://github.com/anees-hill/helper_scripts.git
cd helper_scripts

sudo bash bootstrap.sh --ide
```
